.DS_Store

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Dependency directories
node_modules/

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# dotenv environment variables file
.env*
!.env.sample

# Python (microsserviço sicoob)
python/sicoob_service/.venv/
python/sicoob_service/**/__pycache__/
python/sicoob_service/**/*.egg-info/
.python-version
.pytest_cache/


# certificados (arquivos sensíveis — nunca versionar)
certificados/

# dados sensíveis de teste
docs/boleto.md

# Claude Code
.claude/

# Capturas de tela e gravações (painel Meta, NFS-e, material bruto de tutorial).
# Pasta única, FORA do git: os prints mostram IDs de conta, meio de pagamento,
# PINs e fragmentos de ACCESS_TOKEN, e este repositório é público.
# ATENÇÃO: por não estar no git, esta pasta NÃO tem backup — ver docs/capturas.md.
docs/capturas/
